export type Worker = {
    id: number,
    firstName: string,
    lastName: string,
    location: string,
    job: string,
    charge:string
}

export type User = {
    id: number,
    firstName: string,
    lastName: string,
    email: string,
    password: string
}